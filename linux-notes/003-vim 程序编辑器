# 第 9 章 · vim 程序编辑器

这一张不想用大模型总结了，自己记几条命令吧。

`vi 文件名` ：进入一般指令模式，可以做简单的删除操作
`一般指令模式下按 i`：进入编辑模式，可以编辑文件。按`ESC`退回到一般指令模式
`:w`：写入，即保存
`:q`：退出当前缓冲区
`:qa`：退出全部缓冲区
`:qa!`：强制退出全部缓冲区
`:wq`：写入并退出
`n 上下左右键或hjkl键`：一次移动 n 个单位
`0`：返回行首
`$`：返回行尾
`？word`：搜索word
`dd`：一般指令模式下，删除当前行
`yy`：一般指令模式下，复制当前行
`p`：粘贴


补充下非本章的一个知识点：
写命令时如果太长想换行又不想换命令，可以紧接着命令输入`\`然后按`enter`，就可以做到命令不断且换行继续写了
`alias c='clear'`：命令重命名


下面是大模型生成的笔记的一部份，不删了

## 七、重点速记清单(复习就看这)

1. **三大模式**:一般(默认/Esc 回)、编辑(i/a/o)、命令行(:/?)。卡住先按 `Esc`。
2. **移动**:hjkl(方向)、wbe(词)、0^$(行内)、gg G(首末行)、Ctrl+f/b(翻页)。
3. **编辑**:`dd` 删行、`yy` 复制行、`p` 粘贴、`u` 撤销、`.` 重复。
4. **插入**:i 前 a 后,o 下方新行,大写变行首/尾。
5. **查找**:`/` 下 `?` 上,`n` 下一个 `N` 上一个。
6. **替换**:`:%s/旧/新/g` 全文替换,加 `c` 逐个确认。
7. **存退**:`:wq` 存盘走人,`:q!` 不存强退。
8. **行号**:`:set nu` 显示行号,`nG` 跳第 n 行。

---

## 八、常见易错点回顾

- ❌ 不知道在哪个模式就乱按 → **先按 Esc 回一般模式**再操作。
- ❌ 改完直接关终端 → 没保存!要先 `:wq`;改坏了用 `:q!` 丢弃。
- ❌ `dd` 以为是删除不是剪切 → `dd` 内容进寄存器,`p` 能粘回来。
- ❌ `:s/old/new/` 以为替换全文 → 默认只换**当前行第一个**,全文要 `:%s/.../g`。
- ❌ `i` 和 `a` 分不清 → `i` 在光标前插,`a` 在光标后插。
- ❌ 想撤销按了 `Ctrl+z` → vim 里撤销是 `u`,`Ctrl+z` 是把 vim 挂到后台(可用 `fg` 调回)。
- ❌ 复制粘贴不生效 → 确认是在一般模式按 `yy`/`p`,不是编辑模式。
